```
├── actions         # redux 的action
├── components      # 组件
├── containers      # 页面
├── reducers        # redux的reducers
├── router          # react-router路由配置
├── schemas         # normalizr  js库的实体配置
├── selectors       # reselect js库 state的查询选择
├── store           # redux的store配置
│  └── middleware  # redux 的middleware
├── styles          # 样式
│  ├── base        # less的方法和变量，不存在样式
│  └── app.less    # 全局样式
├── test            # 组件测试
├── util            # 工具库
└── app.less        # 入口
```